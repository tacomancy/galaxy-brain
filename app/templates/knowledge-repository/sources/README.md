# Sources

Store source notes and annotations here. They preserve source identity, claims, Source Locators, and provenance; managed source bytes belong under `assets/`.

Create source notes from the matching template—`templates/book.md`, `course.md`, `paper.md`, `web.md`, or the `other.md` fallback—under `books/`, `courses/`, `papers/`, or `web/`.
